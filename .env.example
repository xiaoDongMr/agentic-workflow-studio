# PostgreSQL connection used when database.backend is postgres.
# Keep real passwords in local .env or deployment secrets. Do not commit them.
DATABASE_URL=postgresql://agentic_workflow:change-me@127.0.0.1:5432/agentic_workflow_studio

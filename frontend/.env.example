# Frontend API base URL used by langgraph-sdk requests.
# Use '/api' when a reverse proxy forwards the same-domain path to FastAPI.
VITE_API_BASE_URL=/api

# Local dev only: Vite proxy target for '/api' requests.
# Leave empty in production builds.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
